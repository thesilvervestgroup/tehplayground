<?php

// path to store render files
// you should make sure this is inside these php config items:
// * include_path
// * open_basedir
// * safe_mode_include_dir (if using safe mode)
define('RENDER_PATH', ''); // don't forget the trailing slash!
